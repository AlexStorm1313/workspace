environment: podman
deployments:
  containers:
    image: localhost
    pullPolicy: Always
  volumes:
    - alexbrasser-website-website
    - appfusion-dashboard-dashboard
    - automtr-api-api
    - automtr-browser-browser
    - automtr-interface-interface
