{{ if has "dbgate" .Values.services }}
kind: ImageStream
apiVersion: image.openshift.io/v1
metadata:
  name: dbgate
  annotations:
    app.kubernetes.io/managed-by: "Helm"
    meta.helm.sh/release-name: {{ .Release.Name }}
    meta.helm.sh/release-namespace: {{ .Release.Namespace }}
spec:
  lookupPolicy:
    local: true
  tags:
  - name: latest
    from:
        kind: DockerImage
        name: 'docker.io/dbgate/dbgate:latest'
    importPolicy:
      scheduled: true
    referencePolicy:
      type: Source
{{ end }}